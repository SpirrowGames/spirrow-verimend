"""Verimend: documentation/reality reconciliation service for the Spirrow platform."""

# The single source of truth for the version. pyproject.toml declares
# `dynamic = ["version"]` and hatchling reads this assignment at build time
# (see [tool.hatch.version]), so bump it here and nowhere else.
__version__ = "0.1.0"
